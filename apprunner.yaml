version: 1.0
runtime: nodejs22
build:
  commands:
    pre-build:
      - npm ci
    build:
      - npx nest build
    post-build:
      - npm prune --production
run:
  command: npm run start:prod
  network:
    port: 8080
  env:
    - name: NODE_ENV
      value: "production"
